meta {
  name: Chat Send Message
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/chat
  body: json
}

body:json {
  {
    "message": "Analyze AAPL for me",
    "preferred_agent": "sentiment"
  }
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("should have response content", function() {
    expect(res.body.response || res.body.content).to.exist;
  });
}
