meta {
  name: Accuracy Dashboard
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/market/accuracy/dashboard
  body: none
}

query {
  days: 90
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
